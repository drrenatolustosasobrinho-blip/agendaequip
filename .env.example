# =====================================================
# FRONTEND (Vite) - variáveis públicas (exposed ao browser)
# =====================================================
VITE_SUPABASE_URL=sua_url_do_supabase
VITE_SUPABASE_ANON_KEY=sua_anon_key_do_supabase
# Base path para deploy em subdiretórios (ex: /agendaequip no GitHub Pages)
# Deixe vazio para raiz
VITE_BASE_URL=/

# =====================================================
# SERVERLESS (Vercel Functions) - variáveis secretas
# =====================================================
SUPABASE_URL=sua_url_do_supabase
SUPABASE_SERVICE_ROLE_KEY=sua_service_role_key_do_supabase
BOOTSTRAP_PASSWORD=senha_secreta_para_bootstrap_admin

# Observação:
# - VITE_* são usadas no frontend (React) e ficam visíveis
# - SUPABASE_SERVICE_ROLE_KEY e BOOTSTRAP_PASSWORD são APENAS server-side
#   (Vercel Functions) e NUNCA devem ser expostas ao cliente
